{% extends 'base.html.twig' %}

{% block title %}Modifier un animal{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier l'animal : <i><b>{{ animal.nom }}</b></i></h1>
        {{ form(formulaire) }}
        {% if error == "1" %}
            <div class="alert alert-danger alert-dismissible d-flex align-items-center fade show">
                <i class="bi-exclamation-octagon-fill"></i>
                <strong class="mx-2">Erreur!</strong> tu ne peux pas dire que l'animal est stérile si tu n'as pas
                choisi son sexe
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endif %}
        {% if error == "2" %}
            <div class="alert alert-danger alert-dismissible d-flex align-items-center fade show">
                <i class="bi-exclamation-octagon-fill"></i>
                <strong class="mx-2">Erreur!</strong> tu ne peux pas mettre de date d'arrive avant de mettre une date de
                départ
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endif %}
        {% if error == "3" %}
            <div class="alert alert-danger alert-dismissible d-flex align-items-center fade show">
                <i class="bi-exclamation-octagon-fill"></i>
                <strong class="mx-2">Erreur!</strong> Ta date de départ est plus tôt que ta date d'arrivé
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endif %}
        {% if error == "4" %}
            <div class="alert alert-danger alert-dismissible d-flex align-items-center fade show">
                <i class="bi-exclamation-octagon-fill"></i>
                <strong class="mx-2">Erreur!</strong> Ta date de naissance est plus tard que ta date d'arrivé au zoo
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endif %}
    </div>
{% endblock %}